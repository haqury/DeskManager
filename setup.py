from setuptools import setup

setup(
    name='DeskManager',
    version='1.0',
    description='A useful module',
    author='Author Name',
    author_email='haqury@gmail.com',
    packages=['DeskManager'],  # same as name
    install_requires=[],  # external packages as dependencies
)